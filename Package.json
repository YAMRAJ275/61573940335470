# package.json create karo
cat > package.json << 'EOF'
{
  "name": "file-processor-server",
  "version": "1.0.0",
  "description": "File upload with incremental delay",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "fs-extra": "^11.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
EOF