services:
  - type: web
    name: file-processor-server
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: MAX_FILE_SIZE
        value: 83886080